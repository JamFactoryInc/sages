use std::ops::Index;


pub struct HtmlTemplate {

}

enum 

pub const fn generate_template() {
    let file = include_bytes!("err.rs");
    let mut i = 0;
    while i < file.len() {
        let x = file[i];
        i += 1;
    }
}